import {
    Container,
    injectable,
    decorate,
    optional,
    inject,
    interfaces,
    LazyServiceIdentifer
} from "inversify";

const Injectable = injectable;
const Inject = inject;
const Decorate = decorate;
const Optional = optional;

export {
    Container,
    injectable,
    decorate,
    optional,
    inject,
    Injectable,
    Inject,
    Decorate,
    Optional,
    interfaces,
    LazyServiceIdentifer,
}